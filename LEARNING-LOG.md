# LEARNING LOG — session-relay

## 2026-08-25
- 分かったこと:
  - 引き継ぎ書は「事実」は意外と残す。落ちるのは**本人がどう言ったか**（温度・苛立ち・興味）。決定だけ渡すと後任は地雷を踏み直す
  - 人間の発話はセッション全体の0.09%しかない。つまり「全部残す」が現実的な選択肢
  - 今日の失敗の本体は情報の欠落ではなく**配達**だった。良い文書があっても読まれなければゼロ
- まだ分かっていないこと:
  - Codex側で「起動時に自動で引く」導線が作れるか（フック相当があるか）
  - 809セッション3GBの初回インデックスにどれくらいかかるか
- 自分の言葉で説明すると:
  - 引き継ぎ書を書くのをやめて、**過去の自分に質問できるようにする**道具

## 2026-08-26
- 分かったこと（実験で判明）:
  - **射影17.2KBだけで、文脈ゼロの新セッションが会話の全体像を復元できた**。中止の理由を本人の原文のまま列挙し、さらに「直前の発言は未回答の質問だ」と気づいた。＝Phase 0の仮説（原文にこそ温度と地雷がある）が裏づけられた
  - ⚠️ ただし**進捗を過小評価した**（「まだ動く証明はできていない」と判断）。原因は明確で、射影が**人間の発話しか持っていない**から。テストが31件通ったことは AI 側の出力にしかなく、渡っていない
    → Phase 2で「結果の信号」を足す必要がある（各ターンの最後のAI発言、git log、テスト結果など）
  - ❌ **ハーネス跨ぎはまだ未証明**。Codexの認証トークンが期限切れで実行できず（`codex login`が必要）。今回証明できたのは「射影だけで文脈が足りる」ことまで
- まだ分かっていないこと:
  - Codexが同じ射影でどこまで復元できるか（要 `codex login`）
  - 「結果の信号」をどこまで入れると、量を増やさずに過小評価を防げるか
- 自分の言葉で説明すると:
  - 引き継ぎ書を書かなくても、**自分が打った言葉を全部渡すだけで**次のAIは状況を掴める。ただし「何が終わったか」は別途渡す必要がある
- 🏁 **ハーネス跨ぎ成功（2026-08-26）**: Codexが射影だけで、中止の判断とその理由（本人の原文）まで答えた。Claude（新セッション）とCodexの2つで再現。中心仮説は実証済み
- **スキルは既に持ち運べた（2026-08-26）**: Claude CodeとCodexは同じ `SKILL.md` 形式（YAMLフロントマター＋Markdown）を使い、Codexは `~/.codex/skills` を読む（本人に列挙させて確認）。MulmoTerminalが自分の10本を両方へ同期していた（`.mt-mirror` = "managed by mulmoterminal"）が、自作8本はClaude側のみだった
  → 対応はシンボリックリンク7本だけ（前のアプリは中止済みのため除外）。**作るものはゼロ**。search-firstの型が効いた事例
  → ⚠️ 未検証のリスク: `~/.codex/skills` はMulmoTerminalが管理しており、同期時に管理外のリンクが消される可能性がある。消えたら貼り直す
  → 学び: 「持ち運ぶ」対象は会話だけではない（スキル・設定・メモリ）。ただしスキルは既に解決済みで、**残る本丸は会話の文脈**

## 2026-08-26 「自分の道具を自分で1回使う」だけでバグが2件出た
`relay` は作った直後は緑だった。実際にrelayで開いた会話をもう一度relayしたら、
前回の文脈がまるごと「本人の発話1件」として入り、渡すほど膨らむ形になっていた。
**作った直後のテストは"自分が想定した使い方"しか通っていない**。1回本番の使い方をすると出る。

もうひとつ。ハーネスが差し込む `<task-notification>` を本人の発話として数えていた。
「人間の発話は1件も落とさない」という方針は、裏返すと**人間でないものを混ぜない**方針でもある。
片方だけ守っていた。

技術面の持ち帰り: 入れ子になりうるテキスト形式は、**中身を字下げして囲う**と外側と混ざらない。
見出しや番号で区切る形式は、中身に同じ見出しが出た瞬間に壊れる（実際に発話が全部消えた）。

## 2026-08-26 「足りない」と分かっていることは、まず量を測ってから足す
結果の信号を足す前に、実データで量を測った。AIの発言を全部入れると110KB、
ターンの最後だけでも77KB、直近10件なら15KB。**測ったから10件に決められた。**
測らずに「全部入れよう」としていたら、射影は6倍になって使い物にならなかった。

## 2026-08-26 同じディレクトリで会話を2つ動かすと、道具は取り違える
`relay` は「このディレクトリで一番最後に書かれた会話」を選んでいた。
別のセルでもう1つ動いていて、それが**17秒新しかった**だけで、そちらを掴んだ。
更新時刻は「いま自分がしゃべっている会話」の代わりにならない。
Claude Codeは `CLAUDE_CODE_SESSION_ID` を環境変数で渡してくる。**当て推量の前に、渡されている値を探す。**

## 2026-08-26 未追跡ファイルに `git checkout` は効かない
Mutation Testでわざと壊したあと `git checkout -- <file>` で戻したら、
新規ファイル（まだ add していない）は「pathspec did not match」で戻らず、変異が残ったまま次の変異を重ねた。
さらに、追跡済みファイルのほうは**その回の作業ごとコミット時点まで巻き戻った**。
壊す実験をするなら、先にコミットするか、コピーを取ってから壊す。
