# LEARNING LOG — session-relay

## 2026-08-29 覚えたものは、覚え方を変えた瞬間に古くなる

見出しから予約実行の自動文を外したのに、一覧には35件そのまま残った。
記録は変わっていないので**更新時刻では気づけない**。変わったのはこちらの作り方だった。

覚え書きに `shape`（版）を持たせ、上げたらまるごと捨てるようにした。
速くする仕組みを入れるときは、**「何が変わったら古くなるか」を一緒に決める**。
記録の更新時刻だけを見ていると、自分のコードの変更が視野に入らない。

## 2026-08-29 短い名前は、いつか必ずぶつかる

一覧の `ref` は8文字。表示には十分だが、**ファイル名にした瞬間に一意性の話になる**。
160件書いて置かれたのは156件——4件が上書きで消えていた。エラーは出ない。
「書いた数」と「置かれた数」を**両方数えて初めて**気づいた。

## 2026-08-29 「人が打った」フラグは、人が話したことを意味しない

予約実行の記録は `promptSource: "typed"` で入ってくる。フラグ上は人の会話だが、
中身は全部ハーネスの流し込みで、本人は一言も言っていない。
一覧に25本並んで初めて分かった。**フラグの名前を信じず、中身を見る。**

## 2026-08-29 一番よく打つ言葉が、一番大きい動作に落ちていた

`relay --help` が**新しいセッションを起動していた**。知らない指定を黙って読み飛ばす作りで、
「何も指定が無い＝いまの会話を渡す」に流れ込んでいたため。`--lst` の打ち間違いも同じ。

作った本人は `--help` を打たない。だから**動くところしか確かめていなかった**。
使いやすくするというのは、まず**間違えたときに何が起きるか**を見ることだった。

## 2026-08-29 速くする前に、どこで時間を使っているかを測る

一覧が3.5秒。「記録を読むから遅い」と思っていたが、実際に効いたのは**2か所**で、
片方は見出しを作る読み込み、もう片方は**作業ディレクトリを嗅ぐための32KB読み**だった。
後者を放置したままだと、「続きから」だけ3.4秒のまま残る（実際に一度そうなった）。

覚えていいものと、いけないものの線も測って決めた——**終わった会話は変わらない**ので覚える。
**いまの会話は続いている**ので毎回読む。

## 2026-08-29 パイプで切られるのは普通の使い方

`relay --projects | head` が EPIPE で落ちて、Nodeのスタックトレースが画面に出た。
自分では最後まで出力させるので気づかない。**途中でやめられること**も動作のうち。

## 2026-08-25
- 分かったこと:
  - 引き継ぎ書は「事実」は意外と残す。落ちるのは**本人がどう言ったか**（温度・苛立ち・興味）。決定だけ渡すと後任は地雷を踏み直す
  - 人間の発話はセッション全体の0.09%しかない。つまり「全部残す」が現実的な選択肢
  - 今日の失敗の本体は情報の欠落ではなく**配達**だった。良い文書があっても読まれなければゼロ
- まだ分かっていないこと:
  - Codex側で「起動時に自動で引く」導線が作れるか（フック相当があるか）
  - 809セッション3GBの初回インデックスにどれくらいかかるか
- 自分の言葉で説明すると:
  - 引き継ぎ書を書くのをやめて、**過去の自分に質問できるようにする**道具

## 2026-08-26
- 分かったこと（実験で判明）:
  - **射影17.2KBだけで、文脈ゼロの新セッションが会話の全体像を復元できた**。中止の理由を本人の原文のまま列挙し、さらに「直前の発言は未回答の質問だ」と気づいた。＝Phase 0の仮説（原文にこそ温度と地雷がある）が裏づけられた
  - ⚠️ ただし**進捗を過小評価した**（「まだ動く証明はできていない」と判断）。原因は明確で、射影が**人間の発話しか持っていない**から。テストが31件通ったことは AI 側の出力にしかなく、渡っていない
    → Phase 2で「結果の信号」を足す必要がある（各ターンの最後のAI発言、git log、テスト結果など）
  - ❌ **ハーネス跨ぎはまだ未証明**。Codexの認証トークンが期限切れで実行できず（`codex login`が必要）。今回証明できたのは「射影だけで文脈が足りる」ことまで
- まだ分かっていないこと:
  - Codexが同じ射影でどこまで復元できるか（要 `codex login`）
  - 「結果の信号」をどこまで入れると、量を増やさずに過小評価を防げるか
- 自分の言葉で説明すると:
  - 引き継ぎ書を書かなくても、**自分が打った言葉を全部渡すだけで**次のAIは状況を掴める。ただし「何が終わったか」は別途渡す必要がある
- 🏁 **ハーネス跨ぎ成功（2026-08-26）**: Codexが射影だけで、中止の判断とその理由（本人の原文）まで答えた。Claude（新セッション）とCodexの2つで再現。中心仮説は実証済み
- **スキルは既に持ち運べた（2026-08-26）**: Claude CodeとCodexは同じ `SKILL.md` 形式（YAMLフロントマター＋Markdown）を使い、Codexは `~/.codex/skills` を読む（本人に列挙させて確認）。MulmoTerminalが自分の10本を両方へ同期していた（`.mt-mirror` = "managed by mulmoterminal"）が、自作8本はClaude側のみだった
  → 対応はシンボリックリンク7本だけ（前のアプリは中止済みのため除外）。**作るものはゼロ**。search-firstの型が効いた事例
  → ⚠️ 未検証のリスク: `~/.codex/skills` はMulmoTerminalが管理しており、同期時に管理外のリンクが消される可能性がある。消えたら貼り直す
  → 学び: 「持ち運ぶ」対象は会話だけではない（スキル・設定・メモリ）。ただしスキルは既に解決済みで、**残る本丸は会話の文脈**

## 2026-08-26 「自分の道具を自分で1回使う」だけでバグが2件出た
`relay` は作った直後は緑だった。実際にrelayで開いた会話をもう一度relayしたら、
前回の文脈がまるごと「本人の発話1件」として入り、渡すほど膨らむ形になっていた。
**作った直後のテストは"自分が想定した使い方"しか通っていない**。1回本番の使い方をすると出る。

もうひとつ。ハーネスが差し込む `<task-notification>` を本人の発話として数えていた。
「人間の発話は1件も落とさない」という方針は、裏返すと**人間でないものを混ぜない**方針でもある。
片方だけ守っていた。

技術面の持ち帰り: 入れ子になりうるテキスト形式は、**中身を字下げして囲う**と外側と混ざらない。
見出しや番号で区切る形式は、中身に同じ見出しが出た瞬間に壊れる（実際に発話が全部消えた）。

## 2026-08-26 「足りない」と分かっていることは、まず量を測ってから足す
結果の信号を足す前に、実データで量を測った。AIの発言を全部入れると110KB、
ターンの最後だけでも77KB、直近10件なら15KB。**測ったから10件に決められた。**
測らずに「全部入れよう」としていたら、射影は6倍になって使い物にならなかった。

## 2026-08-26 同じディレクトリで会話を2つ動かすと、道具は取り違える
`relay` は「このディレクトリで一番最後に書かれた会話」を選んでいた。
別のセルでもう1つ動いていて、それが**17秒新しかった**だけで、そちらを掴んだ。
更新時刻は「いま自分がしゃべっている会話」の代わりにならない。
Claude Codeは `CLAUDE_CODE_SESSION_ID` を環境変数で渡してくる。**当て推量の前に、渡されている値を探す。**

## 2026-08-26 未追跡ファイルに `git checkout` は効かない
Mutation Testでわざと壊したあと `git checkout -- <file>` で戻したら、
新規ファイル（まだ add していない）は「pathspec did not match」で戻らず、変異が残ったまま次の変異を重ねた。
さらに、追跡済みファイルのほうは**その回の作業ごとコミット時点まで巻き戻った**。
壊す実験をするなら、先にコミットするか、コピーを取ってから壊す。

## 2026-08-27 道具が使われずに迂回されたら、それは「使いにくい」ではなく故障
「続きから」と言われて `relay --list` を出したが、見出しが判別できず、
AIはそのまま **JSONL を自分で読んで一覧を作り直した**。作業自体は前に進んだので、
放っておけばバグとして記録されずに終わっていた。
**道具を迂回して同じ結果を出せてしまうと、故障が成功に見える。** 迂回した時点で数える。

原因は、コードに書いてあった前提そのものだった（`最初の発話が一番「何の話か」を表す`）。
画像をドロップすると絶対パスが発話として記録されるので、前提が崩れる。
**自分で書いた前提コメントは、実データで一度は裏を取る。**

もうひとつ。パスを丸ごと消したら、今度はスクショ1枚だけの会話が全部「(発話なし)」になった。
**ノイズを消す修正は、消しすぎて別の「見分けがつかない」を作る。** 消した結果も同じ実データで見る。

## 2026-08-28 「作るか」の前に「単位が合っているか」を疑う
一覧の見出しを直した直後に、本人から「**選択するチャットも取り組んでいるプロジェクト名ではない**」と言われた。
見出しの作り方は直っていたが、**選ばせる単位が間違っていた**。
表示を直すと、単位の誤りが「もう少しで良くなりそう」に見えて隠れる。

先に世の中を見たら、跨ぐ道具はどれも会話を単位にしていなかった（AGENTS.md、Memory Bank、repomix）。
**自分の道具の単位がおかしいときは、世の中の単位を見ると一発で分かる。**

## 2026-08-28 「Miroのように」と言われても、器から作らない
キャンバスは既にある（Flowith、tldrawのキット、JSON Canvasという公開仕様）。
足りないのは**線を流れる中身**の方で、そこは既に自分が持っていた。
JSON Canvasを書き出すだけにしたので、画面は1行も書いていない。
2026-08-26にスマホアプリで下した判断（**先に器を決めると、中身を器に合わせることになる**）と同じ。

## 2026-08-28 数えるものを間違えると、静かに空になる
プロジェクト一覧が2件しか出ず、原因は「新しいファイル480本」で切っていたこと。
実測すると**469本が1つのプロジェクトの下請けの記録**で、人の会話は7本しか残っていなかった。
エラーは出ない。**数が少ないだけの正常終了**として通る。
数える対象（ファイル）と欲しいもの（人の会話）がずれていたら、必ず内訳を出して確かめる。

## 2026-08-28 赤くなったら、まず自分のせいかを切り分ける
実データのテストが落ちたとき、直前に触っていたのは自分だった。
変更を `git stash` して走らせたら**同じように赤かった**（380MBのセッションを読んでいた）。
切り分けずに直していたら、自分の変更を疑って別の場所を壊していた。


## 2026-08-28 「繋がった」は接続のことで、呼べたことではない

MCPは `claude mcp list` が **✔ Connected** と出た時点で終わった気になる。
実際にはそこから、**AIがツールを見つける**／**呼ぶ**／**承認される**／**中身が返る**
の4段があり、Codexは3段目で落ちた（0秒で `user cancelled MCP tool call`）。
接続の緑は、道具が使われた証拠にならない。

## 2026-08-28 返す相手がAIなら、次の一手を文面に書く

一覧をAIに返すとき、`ref` を書かない版は「AIがもう一度自分で探しに行く」ことになる。
人間向けの表は桁を揃えるが、AI向けは**次の呼び出しに必要な値**を必ず1行に入れる。
`list_projects` の末尾に「会話まで降りるには list_conversations に名前を渡す」と書いたのも同じ理由。

## 2026-08-28 標準出力が通信路になったら、`console.log` は事故

MCPのstdioは標準出力がJSON-RPC。デバッグのつもりで1行書くと通信が壊れる。
探索処理を `src/query.ts` に切り出すとき、**画面への出力を全部呼び出し側に残した**のはこのため。
「共通化してついでにログも移す」が一番危ない。

## 2026-08-28 古いビルドが黙って使われると、「直った」が嘘になる

`bin/relay.js` を「dist があれば dist」で書いたら、開発中も**古い dist** が使われた。
修正後に一覧を出したら直って見えたが、変わったのは**データの方**（会話が1本増えた）で、
走っていたのは修正前のコードだった。危ういのは、確認が**通ってしまった**こと。

直し方は優先順位の逆転:**src があれば src**。配布物には src を入れていないので、
入れた人は必ず dist を読む。ビルド忘れは開発側では起きなくなる。

教訓としては前に書いた「道具を迂回すると故障が成功に見える」と同じ形——
**確認したつもりの経路が、確認したい対象を通っていない**。

## 2026-08-28 配れる形は「動くこと」とは別に確かめる

`npm pack` → 別ディレクトリに install → そこから実行、まで通して初めて分かることがある。
今回は **Nodeが `node_modules` の中では型を剥がさない**（`ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING`）。
手元では `.ts` のまま動いていたので、ビルドの要否は**配ってみるまで見えなかった**。
