# 進捗ボード — session-relay（2026-08-25）

## ✅ 完了したこと
- [x] **Phase 0完了**（2026-08-25）: 引き継ぎ書の壊れ方を自分の実データで実測
  - 事実は11項目中8項目残存。落ちたのは本人の原文（温度・地雷）
  - 1セッション13MB中、人間の発話は11.8KB＝**0.09%**（実測）
  - 今日の失敗の正体は欠落でなく**配達**（良い引き継ぎ書が読まれなかった）
- [x] プラン承認・プロジェクトの器作成

- [x] **Phase 1完了**（2026-08-25）: 抽出器（射影）
  - Claude Code / Codex 両形式に対応。`src/types.ts` `parse.ts` `extract-claude.ts` `extract-codex.ts` `extract.ts` `cli.ts`
  - テスト**31件**緑（正常/Edge/Corner/Error＋実データ統合テスト）
  - 厳格lint（`any`・型アサーション禁止、複雑度10・関数40行・ファイル250行）＋strict型チェックが緑
    - 導入直後に複雑度12/16と42行を検出→分割リファクタで解消（ルールが即仕事をした）
  - **Mutation Test（手作業）**: 文字列content分岐をわざと壊すとテスト3件がred→戻すと31件green。回帰ガードが本当に効くことを確認
  - **実測**: 今日のセッション13MB → 人間の発話**70件・5KB**（注入10件は印をつけて除外）／触ったファイル44件・実行コマンド110件・ツール36種

- [x] **射影だけで文脈が足りることを実証**（2026-08-26）: 17.2KBの射影のみを文脈ゼロの新セッションに渡し、`/tmp`で実行（引き継ぎ書・メモリ・リポジトリなし）。プロジェクトの全体像・中止理由（本人の原文）・**未回答の質問があること**まで復元した。証跡=evidence/context-sample.md

- [x] 🏁 **ハーネス跨ぎの引き継ぎに成功**（2026-08-26）: 射影17.8KBのみをCodexに渡し（引き継ぎ書なし）、①現在のプロジェクト ②前のアプリ中止とその理由（「価値はない」を含む）③妥当な次の一手、を正しく回答。Codexは自ら「秘密情報の扱い」を課題に挙げた
  - 証跡: evidence/cross-harness-codex-2026-08-26.md
  - **プロジェクトの中心仮説はこれで実証された**（ただし手動でcontextを渡す粗い形。MCP＋自動配達はPhase 3）

- [x] 🎯 **`relay` コマンド完成**（2026-08-26）: `relay` の1語で、いまの会話を新しいセッション（別スレッド／別ハーネス）で開ける
  - cwdから現在の会話を自動特定（`src/sessions.ts`）→ 射影（`src/context.ts`）→ 対象ハーネスを起動
  - `~/.local/bin/relay` にインストール済み。`relay` / `relay --to codex` / `relay --print`
  - **目的は「Codexに渡す」ではなく「会話を千切れさせずに移る」**（別スレッドも同じ扱い）

- [x] 🐛 **relayを実際に使って出たバグを2件修正**（2026-08-26）: relayで開いた会話をもう一度relayして実測
  - **入れ子**: 前回の文脈がまるごと「本人の発話1件」になり、渡すたびに膨らんでいた → 元の並びに**平らに畳む**（`src/relay-block.ts`）
  - **通知の混入**: `<task-notification>` と `[Request interrupted by user]` を本人の発話として数えていた → 注入として除外
  - 形式変更: 発話の2行目以降を字下げして書く。何段入れ子になっても外側と混ざらない（旧形式も1段は読める＝過去の会話が読めなくならない）
  - テスト**49件**緑（+18件）。厳格lintが `!` を即エラーにし、二重入れ子の取りこぼしはテストが捕まえた
  - 実測（この会話で `relay --print`）: 発話**69件が平ら**・見出しの重複0・通知の混入0・ファイル44件とコマンド20件も引き継ぎ

- [x] ✅ **Phase 2「結果の信号」完了**（2026-08-26）: 渡された先が進捗を過小評価する問題を解消
  - 各ターンの**最後にAIが報告した文**（直近10ターン）＋**渡した時点のgit**（場所・ブランチ・直近10コミット・未コミット）を射影に追加
  - 量の実測で決めた: AIの発言 全部=110KB / ターンの最後だけ=77KB / **直近10件=15KB**。射影は16.7KB→20KB
  - 渡すたびに経過が消えないよう、文脈ブロックから**経過も読み戻す**（`## 直近の経過`）
  - Codexも `agent_message` から同じ形で採る＝ハーネス跨ぎで同じ引き継ぎになる
  - **実証**: 文脈ゼロの新セッションが、テスト件数・コミットハッシュ・作業ツリーの状態まで正しく answered。証跡=evidence/progress-signal-2026-08-26.md
- [x] 🐛 **会話の取り違えを修正**（2026-08-26）: 同じディレクトリで会話を2つ動かしていると、更新時刻が**17秒新しいだけの別の会話**を掴んでいた（実測）
  - `CLAUDE_CODE_SESSION_ID`（Claude Codeが渡してくる）を使って確実に特定。無いときだけ従来の更新時刻に落とす
- [x] テスト**68件**緑（+19件）。Mutation Test 3件とも赤くなることを確認（空白ガード除去→1件red／経過の引き継ぎ除去→2件red／ターンの最後→最初→12件red）

- [x] 🎛 **MulmoTerminalのヘッダーにワンポチを置いた**（2026-08-26・押して確かめるのは本人）
  - `~/.mulmoterminal/config.json` の `buttons` に2つ追加（`POST /api/config` で保存済み・検証で1件も落ちていない）
    - **この会話を別スレッドへ渡す（relay）** … `CLAUDE_CODE_SESSION_ID=${session} relay`
    - **この会話の文脈をコピー** … `… relay --print | pbcopy`（Web Claude / ChatGPT / スマホに貼る用）
  - 既定ボタン（`pick-file` / `pr`）は消えないように書き足してある。`chips` は触っていない
  - **鍵は `${session}`**: MulmoTerminalはセルを `claude --session-id <uuid>` で起動していて、その uuid が Claude Code のセッションIDとそのまま同じ（実測: `~/.claude/projects/-Users-USER-mulmoclaude/2a30ca86-….jsonl` が存在）。だからボタンから渡しても**会話を取り違えない**
    - 実測: cwdが ai-samples のシェルから `CLAUDE_CODE_SESSION_ID=2a30ca86-…` を付けて `relay --print` → ちゃんと mulmoclaude 側の会話（10KB）が出た。付けないと ai-samples の会話（32KB）
  - ⚠️ **未確認**: 実際にヘッダーのボタンを押した動作はまだ見ていない。特に `relay`（新セッション起動）は**コマンドセルで対話TTYが取れるか**が未検証。`--print | pbcopy` の方は端末が要らないので通るはず

- [x] ✅ **`relay --print --previous` 追加**（2026-08-27・別セッションからの依頼）: 「自分ではなく直前の会話」を引く
  - 新しいセッション（スマホからでも）が `relay` スキルで「続きから」と言われたときに使う。自分を除かないと**始まったばかりの空の自分**を読んでしまう
  - IDが分かればそれを除外／分からなくてもClaude Codeの中からなら一番新しい＝自分を落とす／中身の無い会話は飛ばす／該当ゼロなら `exit 1` で明示終了（**空の文脈を返さない**）
  - テスト**79件**緑（+11件）
  - ⚠️ 限界: 「自分以外で最も新しい」なので、**同じcwdで別のセッションが並行して動いていると、そちらを引く**（実測で確認）

## 🔨 いまやっていること
- ヘッダーボタンを**実際に押して**確かめる（上の⚠️）。通れば Phase 3の入口は完了

## ⚠️ いまの限界（分かっていて残していること）
- gitの欄は**relayを打ったディレクトリ**のリポジトリを映す。会話の話題と別のリポジトリのことがある
  （実測: cwdがai-samples、作業はsession-relay）。渡した先も同じcwdで開くので嘘ではないが、`場所:` を明記して区別できるようにした
- 触ったファイルの一覧は Read/Edit/Write の入力からしか採れない。**Bashだけで作業すると空に近くなる**（この会話がまさにそう）
- ✅ **調査済み（2026-08-27）: CodexはセッションIDを環境変数で渡してこない。** `codex` バイナリが持つ `CODEX_` 系は `CODEX_APPLY_GIT_CFG` / `CODEX_CI` / `CODEX_INTERNAL_ORIGINATOR_OVERRIDE` の3つだけ（codex-cli 0.147.0 を実測）
  - 代わりに記録の**先頭行が `type: session_meta`** で、`payload.session_id` と `payload.cwd` を持つ。置き場所は `~/.codex/sessions/YYYY/MM/DD/rollout-<ISO時刻>-<uuid>.jsonl`（ファイル名に時刻とidが入る／実測51件）
  - つまり **Claude Code ほど確実には特定できない**。Codex側は「cwd一致＋新しい順」の当て推量に頼るしかない
  - ただし Codex 自身も `codex resume --last` で「直前」を時刻で決めている。**同じ割り切りで良い**という根拠になる

- [x] スキルのハーネス跨ぎ対応（2026-08-26）: 自作7本を`~/.codex/skills`へシンボリックリンク。Codex側で認識を実測確認。**作るものはゼロだった**
- [x] ロードマップの置き場所を決定（正本はKICKOFF/STATUSの1箇所・READMEは公開時）／中止したアプリ専用スキル削除

## ⏭ 残り
- Phase 4で README にロードマップを載せる（それまで置き場所を増やさない）
- Phase 2の残り: インデックス（SQLite + FTS5）→ Phase 3 MCP＋自動配達 → Phase 4 公開
- Phase 3の設計変更: 「引ける」だけでなく**新セッション起動時に自動で宙に浮いた件を知る**導線（Claude CodeはSessionStartフック、Codex側は要調査）

## ⚠️ ハマりどころメモ
- Claude Codeの`message.content`は**文字列とブロック配列の両方**を取る。片方だけ見ると発話を大量に取りこぼす（実測: 11件しか取れず、正しくは80件）
- `custom-title`が付いているのは**11%**だけ。索引に当てにできない
- Codexは`event_msg`の`payload.type=="user_message"`に`message`が入る。`~/.codex/config.toml`は既に`[mcp_servers.*]`を持つ＝MCP対応済み

## 💡 これからの候補（2026-08-26 追記・未着手）

### A. 普通のClaude/GPTチャットとの相互移動
方向で難易度が真逆。調査済み。
- ~~**① ターミナル → Web チャット（簡単・30分）**~~ → **コードを書かずに解決**（2026-08-26）。ヘッダーボタンで `relay --print | pbcopy`。`--to clipboard` フラグは**作らなかった**
- **② Web チャット → ターミナル（難しい）**: 「1会話だけ、いますぐ」取り出す公式APIが Claude/ChatGPT どちらにも無い
  - 公式データエクスポート = 全会話まとめてZIP・メールで数時間後
  - ブラウザ拡張 = 1会話ずつ即出せるが他人の拡張に依存
  - **自分用の現実解 = Chrome MCPで開いているタブから直接読む**
  - ⚠️ OSSで配るときは「拡張やMCPが要る」＝ハードルになる。正直に書く
- 構造の違い: Webチャットには cwd もファイルもコマンドも無い。運ぶのは会話だけで、**人間の発話の割合はずっと高い**（射影はより単純になる）

### B. スマホのリレーアプリ → ⛔**アプリは作らない。ただし「何も作らなくていい」ではない**（2026-08-26 実測）
実機で検証した結果、**サーバー基盤（Supabase＋APNs＋iOSアプリ）は丸ごと不要**と判明。ただし**手順は実際にひどく面倒だった**（本人の言葉:「めちゃくちゃめんどかった」）。約10手かかっている:
Macがスリープ→電源→Dispatch設定→タブ違い→コマンド打ち間違い→URLで接続→新セッションは会話を知らない→AirDrop→Notes→スマホで全選択コピー→貼る。
**これは成功ではなく、直すべき摩擦**。下の「次に作るもの」がその答え。
- ✅ **操作はRemote Controlで解決**: `claude --remote-control` / `/remote-control` → スマホのClaudeアプリから母艦のセッションに入って会話できる（実測）。移すのでなく同じ会話に別の入口から入るので千切れない
- ⚠️ **ただし新しいセッションは会話を知らない**ので、relayは別途必要（Remote Controlが解いたのは「どこから操作するか」だけ）
- ✅ **受け渡しはApple Notes（iCloud同期）で足りた**: `relay --print` → Notesに入れる → スマホでコピペ。**サーバーもプッシュ基盤も不要**（前のアプリで作ったものが丸ごと要らなかった）
- 🎯 **次に作るもの（摩擦をゼロにする本命）: `relay` スキル**
  スマホから一言「続きから」と打つだけにする。セッションはMacの上で動いているので、**自分で `relay` を実行して前の会話を読める**。ファイルもNotesもコピペも不要（＝今日の10手が1手になる）
  - 必要な追加: **`relay --print --previous`**（新しいセッションが「自分ではなく直前の会話」を引くためのオプション）。現状は `CLAUDE_CODE_SESSION_ID` で自分を掴むので、新セッションからだと空の自分を返してしまう
  - `--to notes` / `--to clipboard` はその後でいい（スキルが効けば出番が減る）
- ⚠️ Remote Controlの弱点（実測）: **Macが起きてオンラインである必要がある**。蓋を閉じると寝る（`sleep 1`設定・caffeinateの`-i`もClaudeのトグルも蓋閉じは防げない）。外出中に使うなら電源＋Capsomnia が前提

### （旧）スマホのリレーアプリ（構想・保留）
スマホから会話を移す／スマホで続ける。
- 🚨 **前のアプリと同じ罠**: セッションの実体はMacにあるので、アプリにするには同期基盤（サーバー＋プッシュ）が要る。前回そこに時間を使って中止した
- **より安い道が既にある**: Claude Code公式の **Remote Control**（Claudeモバイルアプリからセッションを操作できる。ターミナルにも `/remote-control` のヒントが出る）＋ `relay` コマンド
  → スマホから「relayして」と言えば移せる＝**アプリを作らずに8割**
- 判断: まずデスクトップで毎日使う。**スマホ側で本当に足りないことが特定できてから**作る（先にアプリを決めると、また器に中身を合わせることになる）
