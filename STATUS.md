# 進捗ボード — session-relay（2026-09-01更新）

## ✅ 完了したこと
- [x] **Phase 0完了**（2026-08-25）: 引き継ぎ書の壊れ方を自分の実データで実測
  - 事実は11項目中8項目残存。落ちたのは本人の原文（温度・地雷）
  - 1セッション13MB中、人間の発話は11.8KB＝**0.09%**（実測）
  - 今日の失敗の正体は欠落でなく**配達**（良い引き継ぎ書が読まれなかった）
- [x] プラン承認・プロジェクトの器作成

- [x] **Phase 1完了**（2026-08-25）: 抽出器（射影）
  - Claude Code / Codex 両形式に対応。`src/types.ts` `parse.ts` `extract-claude.ts` `extract-codex.ts` `extract.ts` `cli.ts`
  - テスト**31件**緑（正常/Edge/Corner/Error＋実データ統合テスト）
  - 厳格lint（`any`・型アサーション禁止、複雑度10・関数40行・ファイル250行）＋strict型チェックが緑
    - 導入直後に複雑度12/16と42行を検出→分割リファクタで解消（ルールが即仕事をした）
  - **Mutation Test（手作業）**: 文字列content分岐をわざと壊すとテスト3件がred→戻すと31件green。回帰ガードが本当に効くことを確認
  - **実測**: 今日のセッション13MB → 人間の発話**70件・5KB**（注入10件は印をつけて除外）／触ったファイル44件・実行コマンド110件・ツール36種

- [x] **射影だけで文脈が足りることを実証**（2026-08-26）: 17.2KBの射影のみを文脈ゼロの新セッションに渡し、`/tmp`で実行（引き継ぎ書・メモリ・リポジトリなし）。プロジェクトの全体像・中止理由（本人の原文）・**未回答の質問があること**まで復元した。証跡=evidence/context-sample.md

- [x] 🏁 **ハーネス跨ぎの引き継ぎに成功**（2026-08-26）: 射影17.8KBのみをCodexに渡し（引き継ぎ書なし）、①現在のプロジェクト ②前のアプリ中止とその理由（「価値はない」を含む）③妥当な次の一手、を正しく回答。Codexは自ら「秘密情報の扱い」を課題に挙げた
  - 証跡: evidence/cross-harness-codex-2026-08-26.md
  - **プロジェクトの中心仮説はこれで実証された**（ただし手動でcontextを渡す粗い形。MCP＋自動配達はPhase 3）

- [x] 🎯 **`relay` コマンド完成**（2026-08-26）: `relay` の1語で、いまの会話を新しいセッション（別スレッド／別ハーネス）で開ける
  - cwdから現在の会話を自動特定（`src/sessions.ts`）→ 射影（`src/context.ts`）→ 対象ハーネスを起動
  - `~/.local/bin/relay` にインストール済み。`relay` / `relay --to codex` / `relay --print`
  - **目的は「Codexに渡す」ではなく「会話を千切れさせずに移る」**（別スレッドも同じ扱い）

- [x] 🐛 **relayを実際に使って出たバグを2件修正**（2026-08-26）: relayで開いた会話をもう一度relayして実測
  - **入れ子**: 前回の文脈がまるごと「本人の発話1件」になり、渡すたびに膨らんでいた → 元の並びに**平らに畳む**（`src/relay-block.ts`）
  - **通知の混入**: `<task-notification>` と `[Request interrupted by user]` を本人の発話として数えていた → 注入として除外
  - 形式変更: 発話の2行目以降を字下げして書く。何段入れ子になっても外側と混ざらない（旧形式も1段は読める＝過去の会話が読めなくならない）
  - テスト**49件**緑（+18件）。厳格lintが `!` を即エラーにし、二重入れ子の取りこぼしはテストが捕まえた
  - 実測（この会話で `relay --print`）: 発話**69件が平ら**・見出しの重複0・通知の混入0・ファイル44件とコマンド20件も引き継ぎ

- [x] ✅ **Phase 2「結果の信号」完了**（2026-08-26）: 渡された先が進捗を過小評価する問題を解消
  - 各ターンの**最後にAIが報告した文**（直近10ターン）＋**渡した時点のgit**（場所・ブランチ・直近10コミット・未コミット）を射影に追加
  - 量の実測で決めた: AIの発言 全部=110KB / ターンの最後だけ=77KB / **直近10件=15KB**。射影は16.7KB→20KB
  - 渡すたびに経過が消えないよう、文脈ブロックから**経過も読み戻す**（`## 直近の経過`）
  - Codexも `agent_message` から同じ形で採る＝ハーネス跨ぎで同じ引き継ぎになる
  - **実証**: 文脈ゼロの新セッションが、テスト件数・コミットハッシュ・作業ツリーの状態まで正しく answered。証跡=evidence/progress-signal-2026-08-26.md
- [x] 🐛 **会話の取り違えを修正**（2026-08-26）: 同じディレクトリで会話を2つ動かしていると、更新時刻が**17秒新しいだけの別の会話**を掴んでいた（実測）
  - `CLAUDE_CODE_SESSION_ID`（Claude Codeが渡してくる）を使って確実に特定。無いときだけ従来の更新時刻に落とす
- [x] テスト**68件**緑（+19件）。Mutation Test 3件とも赤くなることを確認（空白ガード除去→1件red／経過の引き継ぎ除去→2件red／ターンの最後→最初→12件red）

- [x] 🎛 **MulmoTerminalのヘッダーにワンポチを置いた**（2026-08-26・押して確かめるのは本人）
  - `~/.mulmoterminal/config.json` の `buttons` に2つ追加（`POST /api/config` で保存済み・検証で1件も落ちていない）
    - **この会話を別スレッドへ渡す（relay）** … `CLAUDE_CODE_SESSION_ID=${session} relay`
    - **この会話の文脈をコピー** … `… relay --print | pbcopy`（Web Claude / ChatGPT / スマホに貼る用）
  - 既定ボタン（`pick-file` / `pr`）は消えないように書き足してある。`chips` は触っていない
  - **鍵は `${session}`**: MulmoTerminalはセルを `claude --session-id <uuid>` で起動していて、その uuid が Claude Code のセッションIDとそのまま同じ（実測: `~/.claude/projects/-Users-USER-mulmoclaude/2a30ca86-….jsonl` が存在）。だからボタンから渡しても**会話を取り違えない**
    - 実測: cwdが ai-samples のシェルから `CLAUDE_CODE_SESSION_ID=2a30ca86-…` を付けて `relay --print` → ちゃんと mulmoclaude 側の会話（10KB）が出た。付けないと ai-samples の会話（32KB）
  - ⚠️ **未確認**: 実際にヘッダーのボタンを押した動作はまだ見ていない。特に `relay`（新セッション起動）は**コマンドセルで対話TTYが取れるか**が未検証。`--print | pbcopy` の方は端末が要らないので通るはず

- [x] ✅ **`relay --print --previous` 追加**（2026-08-27・別セッションからの依頼）: 「自分ではなく直前の会話」を引く
  - 新しいセッション（スマホからでも）が `relay` スキルで「続きから」と言われたときに使う。自分を除かないと**始まったばかりの空の自分**を読んでしまう
  - IDが分かればそれを除外／分からなくてもClaude Codeの中からなら一番新しい＝自分を落とす／中身の無い会話は飛ばす／該当ゼロなら `exit 1` で明示終了（**空の文脈を返さない**）
  - テスト**79件**緑（+11件）
  - ⚠️ 限界: 「自分以外で最も新しい」なので、**同じcwdで別のセッションが並行して動いていると、そちらを引く**（実測で確認）

- [x] 🐛 **一覧の見出しが役に立たない問題を修正**（2026-08-27・「続きから」を実地で使って発覚）
  - 症状: `relay --list` の「何の話か」が**判別不能**。15本中6本は最初の発話がスクショの絶対パスだけで、4行が同じ見た目になった。本人の言葉:**「これだとわからんな。。。」**
  - 結局その場では relay を使わず、AIが JSONL を直接読んで一覧を作り直した＝**道具が仕事をしていない**
  - 原因はコードのコメントに書いてあった前提そのもの: `/** 会話の見出し。最初の発話が一番「何の話か」を表す */`。画像をドロップすると**絶対パスが発話として記録される**ので、この前提は崩れる
  - 直したこと（`src/list.ts`）:
    - パス・`[Image: …]`・URLを落として、**中身のある最初の発話**を見出しにする
    - 引用符つきのパスは**名前に空白が入る**（`'…/スクリーンショット 2026-08-25 23.30.34.png'`）ので `\S+` では届かなかった。引用符ごと拾う
    - 1行目だけ見ると空の発話がある（改行で始まる）。**最初の空でない行**を使う。これで1本が「(発話なし)」に退化していた
    - URLはホスト名まで畳む（`raycast.com これ入れようかな`）。URLを貼っただけの発話は飛ばして次を見出しにする
    - ファイル名は**他に何も残らなかったときだけ**使う。先に混ぜると `スクリーンショット 2026-08-25 23.30.34` が44文字の枠を食って本文を押し出した（実測）。一時ファイルの uuid は名前も捨てる
  - **実測（同じ15本で確認）**: パスの残り **6行 → 0行**、見分けのつかない行 **4行 → 0行**。`スクリーンショット 2026-08-24 21.52.32` と `…21.57.19` が別物として並ぶ
  - テスト**141件**緑（+9件）／厳格lint・strict型チェックも緑

- [x] 🗂 **選ぶ単位を「会話」から「プロジェクト」に変えた**（2026-08-28）
  - 発端は本人の指摘:「選択するチャットも取り組んでいるプロジェクト名ではないし、**会話の途切れたところなのでわからない**」
  - 先に世の中を見た（`search-first`）。跨ぐ道具はどれも会話を単位にしていない
    - **AGENTS.md**: リポジトリ直下の1枚。2025年12月にOpenAIがLinux Foundation（AAIF）へ寄贈、6万リポジトリ超・Codex/Cursor/Devin/Copilot/Jules等が対応
    - **Memory Bank**（Cline）: `projectbrief.md` / `activeContext.md` / `progress.md`。**このリポジトリの4点セットと同じ型**
    - **repomix / gitingest**: リポジトリを1ファイルに詰めて任意のAIへ貼る（「プロジェクトを他のAIに繋ぐ」の既製品）
    - **OpenMemory MCP**（mem0）: ローカル完結でClaude/Cursor/Windsurfが記憶を共有。**Phase 3と正面からぶつかる**
    - 会話ピッカーを持つのは `claude --resume` / `codex resume` くらいで、どちらも**同じ道具の中に戻る**用
  - 判断: **会話を運ぶのがこの道具の取り柄**（Phase 0で実測した「温度と地雷」）。世の中が解いていないのはそこ。変えるのは**選ぶ単位**だけ
  - 作ったもの: `src/projects.ts`（cwdで束ねる）／`relay --projects`／`relay --list --in <#|名前>`
  - 🐛 **同時に見つけた実測**: ファイル数で切ると足りない。**新しい480本のうち469本が1つのプロジェクトの下請け**で、人が打った会話は7本しか残らずプロジェクト一覧が2件になった。数えるのは会話であってファイルではない（`SCAN_LIMIT`）
  - 🐛 **番号とrefで数える相手を変えた**: 番号は「画面に出ていた一覧」でしか意味を持たない。refは一意なのでSCAN全体から探す。**キャンバスのカードに書くのはref**（実測: 8/14の会話＝新しい40件の外でも当たる）
  - ハーネスが差し込むタグ（`<scheduled-task …>`）を落として、人が書いた中身を見出しにする

- [x] 🎨 **`relay --canvas` を追加（画面は作らない）**（2026-08-28）
  - 本人の要望:「Miroのように繋げられたらいい」。世の中には Flowith（無限キャンバスAI・100万ユーザー）や tldraw の branching-chat キットがあるが、**繋がるのはその道具の中の会話同士**で、他のAIへ渡す線ではない
  - **器を先に作らない**（2026-08-26のスマホアプリと同じ判断）。[JSON Canvas](https://jsoncanvas.org/)（Obsidianのキャンバス形式・MIT・公開仕様）を書き出すだけにした。ドラッグも矢印もObsidianの既存機能
  - 会話のカードに `relay --from <ref>` を書く。**見えるだけのカードは結局どこかで打ち直すことになる**
  - 実測: プロジェクト12 / 会話181 → ノード193・線181、id重複0・繋ぎ先の欠け0・JSON Canvasの必須項目欠け0
  - ⚠️ 限界: `.canvas` で見えても、**線を引いた先のAIに自動で流れるわけではない**。そこはPhase 3（MCP）

- [x] 🐛 **実データテストが380MBのセッションを掴んで赤かったのを修正**（2026-08-28・この変更とは無関係の既存不具合）
  - 「一番大きいファイル」を選ぶ作りで、手元の**380MB**の記録を丸ごと読んで5秒制限を超えていた。変更を退避しても赤いことを確認してから直した
  - 上限（64MB）内で一番大きいものを使う。**テスト全体が32秒→1.4秒**になった
  - テスト**163件**緑（+22件）／厳格lint・strict型チェックも緑

- [x] 🔌 **Phase 3: MCPサーバー**（2026-08-28）: 貼るのをやめ、**受け取る側のAIが自分で取りに来る**形にした
  - `src/mcp.ts`（当時の道具3つ＋プロンプト`resume`）／`src/mcp-answers.ts`（返す文面）／`relay mcp` で起動
  - 公式SDK（`@modelcontextprotocol/sdk` 1.30）を使う。KICKOFFの「公式の手順はそのまま再現する」に従った。**初めての依存**（それまで0）
  - `src/query.ts` を切り出した。CLIとMCPが**同じ探索処理**を使う（番号とrefの数え分けもここ）
  - **実測（本物のMCPクライアントで接続）**: `list_projects` 3.5秒/20件・`list_conversations` 2.8秒・`get_context` 引数なし **0.4秒/52KB**・当たらないrefは `isError` で返る
  - 置き先: Claude Code（`claude mcp add relay --scope user`）は **✔ Connected**／Codex（`codex mcp add relay`）も登録済み
  - ⚠️ **Codexの headless は承認で落ちる**（実測）: `codex exec` はツールを見つけて `mcp__relay__get_context` を呼ぶところまで行くが、**0秒で `user cancelled MCP tool call`**。`approval_policy="never"` でも同じ。Codex側の判定なので、**対話TUIで本人が承認する形なら通るはず（未確認）**
  - テスト**173件**緑（+10）。うち3件は**本物のMCPクライアントでstdio接続**して道具が見えることを確認する（中身の走査はテストに入れない——数秒に膨らむ）
  - 📌 **npmの `session-relay` は別人が使用中**（1.3.2・中国語の「会话接力」＝同じ領域の道具）。配布名は未決

- [x] 📦 **npmで配れる形にした**（2026-08-28）: 名前は **`@shoujiki-panman/session-relay`**（素の名前は `session-relay` / `relay-session` / `ai-relay` / `session-handoff` / `handoff-relay` すべて他人が使用中。スコープ付きなら名前を変えずに済む）
  - `npm run build`（`tsconfig.build.json`・`rewriteRelativeImportExtensions` で `./x.ts` → `./x.js`）／`bin/relay.js`（Windowsでも動く入口）／`files` は dist・bin・skills だけ
  - **`src` の `.ts` は配らない**。Nodeは `node_modules` の中では型を剥がさない（`ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING`）ので、TSのまま配ると入れた人の環境で動かない
  - **当時の実測**: `npm pack` → 別ディレクトリに install → `relay --projects` が動き、MCPも道具3つ・`get_context` 51KB/781ms・`list_projects` 3.5秒で返った
  - まだ **publish していない**（名前だけ決めた状態）

- [x] 🐛 **プロジェクトの見出しが「(発話なし)」になっていたのを修正**（2026-08-28）
  - 一番新しい会話が空（起動しただけ・スクショ1枚）だと、**プロジェクトごと「(発話なし)」**に見えて選べない。`headline()` で中身のある会話から採る
  - 破壊テスト済み（`sessions[0].topic` に戻すと2件赤 → 戻すと緑）

- [x] 🧯 **打ち間違いが会話を起動していたのを直した**（2026-08-29）: `relay --help` も `relay --lst` も、**黙って新しいClaudeセッションを起動していた**（実測・文脈53KB付き）。知らない指定を読み飛ばす作りだったため、**一番よく打つ言葉が一番大きい動作**に落ちていた
  - `src/usage.ts`: 使い方の表示＋知らない指定の検出（値を取るフラグの直後は値なので叱らない）。`--help` / `-h` / `help` は使い方、知らない指定は使い方を出して `exit 2`
- [x] 🧰 **`relay install` を足した**（2026-08-29）: 手で入れると4コマンド（claude / codex / スキル2本）。しかも**MCPは絶対パスで書かないと動かない**ので、間違えると「繋がらない」だけが残る
  - 入っているハーネスだけを見る／2度目は何もしない／`--dry-run` で見るだけ
  - 🐛 テストが設計の穴を捕まえた: **ハーネスが入っていないのにスキルだけ置こうとしていた**（使う人のいない `~/.claude/skills` を作る）
- [x] ⚡ **一覧を覚えるようにした**（2026-08-29）: 会話は終われば変わらないので、更新時刻が同じなら読み直さない（`~/.cache/session-relay/list.json`）
  - 効いたのは2か所。**見出しを作るための読み込み**と、**作業ディレクトリを嗅ぐための32KB読み**（4000本ぶん）。後者は `sessionsFor` にも渡した
  - **実測（MCP越し）**: `list_projects` 3459ms → **49ms**／`list_conversations` 2761ms → **44ms**／`get_context` は毎回読むので 2.3秒（会話は続いているので覚えたら古くなる。ここは速くしない）
  - 壊れていたら黙って捨てる・消えた記録は書き出さない・形が違えば読まない（テスト8件）
- [x] 🧯 **パイプで切るとスタックトレースが出ていたのを直した**（2026-08-29）: `relay --projects | head` が EPIPE で落ち、**Nodeのスタックトレースが画面に出ていた**（`src/pipe.ts`）
- [x] テスト**198件**緑（+8）／厳格lint・strict型チェックも緑

- [x] 🗣 **コマンドを打たずに済むことを実測した**（2026-08-29・本人の指摘「エンジニアじゃないからコマンド形式は使いにくい」）
  - 新しいClaudeセッションに **「前の会話で私が実際に打った言葉を3つ、原文のまま」** → relayを自分で呼び、ref `c44a44e9` から原文を返した（リポジトリには無い情報なので、relayを使った証拠になる）
  - 新しいセッションに **「キャンバスの話の続きをやりたい」** → 当の会話を見つけ、「`.canvas` は見えるだけで文脈は流れない＝ここが分岐点」まで答えた
  - 🐛 **その最初の実測で、AIが「番号か ref を言ってください」と聞き返した**。打つのが苦手な人にrefを言わせるのは、コマンドを打たせているのと同じ
    - `get_context` に `about`（本人の言葉）を足した。1本に決まればそのまま渡す／複数なら候補を返す／**refを本人に聞き返すな**と道具の説明と返答の両方に書いた
- [x] テスト**201件**緑

- [x] 📱 **MulmoTerminalに一覧を出した**（2026-08-29・本人が選んだ入口）
  - `relay --records <場所>` で1会話＝1ファイルに書き出す（`src/records.ts`）→ MulmoTerminalのコレクションが読む
  - `skills/relay-chats/`（schema.json ＋ views/chats.html）。**実体はこのリポジトリに置き、ワークスペースへコピーする**——`data/` はgit管理外なので、ここに置かないとビューが消える
  - タップすると `__MC_VIEW.startChat` で**新しいチャットが下書き付きで開く**（送信は本人。押しただけでは何も起きない）
  - 画面は**自分で作っていない**。MulmoTerminalのコレクションとカスタムビューに乗せただけ
  - 🐛 **refが8文字で衝突していた**: 160件書いて置かれたのは156件。**4件が上書きされて消えていた**。idを記録ファイル名にして、refは別項目に分けた
  - 🐛 **一度も人が話していない会話が25本並んでいた**（予約実行。`promptSource` は typed だが中身は全部ハーネスの流し込み）。一覧から外した
  - 🐛 **覚え書きに版が無く、直したのに直らなかった**: 見出しの作り方を変えても、更新時刻が同じ記録は古い見出しのまま返ってきた（35件）。`shape` を持たせて、上げたらまるごと捨てる
  - ⚠️ **画面が出るところは本人の確認待ち**。レコード160件・schema・ビューのコピーまでは確認したが、描画は見ていない
- [x] テスト**211件**緑

- [x] 📮 **スマホの普通のClaudeチャット用「投函口」をローカルで実証**（2026-08-31）
  - Remote Controlではない普通のClaudeチャットはMacのJSONLに無く、従来のrelayから読めない。本人が「relayに預けて」と明示した会話だけを受ける境界を追加
  - `relay mcp-deposit`: 外向き候補のMCP。見える道具は **`deposit_conversation` 1つだけ**。ローカル会話の一覧・読み取りは一切置かない
  - `relay mcp`: Mac側に `list_deposits` / `get_deposit` を追加。refなしなら最新を読み、同じrelay文脈形式で本人の原文と直近の経過を返す
  - 保存先は `~/.local/share/session-relay/inbox/`。ディレクトリ `0700`・ファイル `0600`、入力は1件64KB・全体256KB・発話200件まで。ファイル名はサーバー生成UUIDで、本人の題名をパスに使わない
  - **本物のMCPクライアント2台で往復を実測**: 投函専用MCPへ原文2件を預ける → 通常MCPが最新を読み、2件とも原文のまま返した
  - テスト**229件**緑（+8件）／lint・strict型チェック・buildも緑
  - ⚠️ まだローカルstdioだけ。Claudeモバイル用の公開URL・Streamable HTTP・OAuthは作っていない。**認証なしで公開しない**

- [x] 🔐 **投函口をHTTPにして、Cloudflare Accessの署名を自分でも検証するようにした**（2026-09-01）
  - `relay mcp-deposit-http`（`src/deposit-http.ts` / `deposit-http-main.ts`）: Streamable HTTPの投函口。**127.0.0.1にしかbindしない**（外に出すのはCloudflare Tunnelの役目）
  - `src/cloudflare-access.ts`: `Cf-Access-Jwt-Assertion` を **公開鍵（`/cdn-cgi/access/certs`）・issuer・audience** まで検証（`jose`）。**Accessを通っただけで信用しない**＝Tunnelの設定を誤って認証なしにしてもMCP本体が拒む
  - チームドメイン（`SESSION_RELAY_ACCESS_TEAM_DOMAIN`）とAUD（`SESSION_RELAY_ACCESS_AUD`）が無ければ**起動しない**
  - 🐛 **DNS rebindingのテストが実は何も試していなかった**: `fetch` では `Host` は禁止ヘッダーで**偽装できない**。403のつもりが406（Acceptヘッダー不足）で落ちていた＝middlewareまで届いていない。`node:http` の生リクエストに書き換え、「正しいHostなら403にならない」裏取りも足した（常に403になる作りだと緑になってしまう）
  - 🐛 **SDK 1.30のNode版transportは strict型チェックを通らない**: `onclose` 等が `| undefined` 付きで宣言されていて、`exactOptionalPropertyTypes` の `Transport` に渡せない（clientの `sessionId` も同じ）。castは禁止しているので、その2行だけ `@ts-expect-error` に理由を書いた（**SDKが直ったらこの行がエラーになって気づける**）。ステートレスは `sessionIdGenerator: undefined` を渡す代わりに**キーを渡さない**形にした（実行時は同じ）
  - **実測**: 本物のMCPクライアントでHTTP越しに接続 → 道具は `deposit_conversation` **1つだけ**・原文2件が保存・JWT無しは**403**・偽Hostは**403**。テスト**241件**緑／lint・strict型チェック・buildも緑
  - **破壊テスト**: Access検証のmiddlewareを外すと1件red → 戻すと6件green
  - ⚠️ **まだCloudflare側は触っていない**。Tunnelを張る・AccessアプリとAUDを作る・Claudeモバイルのカスタムコネクタに登録する、は未実施

- [x] 🌐 **Cloudflare側を構築し、スマホ実機E2Eに成功**（2026-09-01・複数セッション合作）
  - ドメイン **shoujiki-panman.com** を購入（$10.46/年・自動更新OFF＝**2027-09-01失効。来年8月に更新判断**）
  - Tunnel `relay-deposit` → `relay.shoujiki-panman.com` → 127.0.0.1:8788（`~/.cloudflared/config.yml`・**httpHostHeader: 127.0.0.1が必須**。無いとMCP側のHost検査が403にする）
  - Zero Trust Free（チーム名 `shoujiki-panman`）／MCP Portals > MCP serversに登録（Auth type=None）／ポリシー `only-tanuma`（Emails=本人のみ）
  - 🐛 **AccessアプリのSubdomain/Domainが空のままだと門番が立たない**（実測: 素通りしてMacの403が返った＝二段目の守りが仕事をした）。`relay`+`shoujiki-panman.com` を設定して解決
  - 検証条件の置き場: `~/.config/session-relay/deposit-http.env`（TEAM_DOMAIN・AUD）
  - **実機E2E**: Web版Claude（合成データ）→iPhone版Claude（実会話）の両方でMacへ投函成功。未認証は401、JWTなし/無効/偽Hostは403。証跡=evidence/claude-mobile-e2e-2026-09-01.md（個人情報・実URLは載せない）
- [x] 📦 **v0.2.0の公開準備**（2026-09-01・Codexセッションが枠切れまで進め、このセッションが引き継ぎ）
  - README再構成（最短で試す／実機確認済みの記述へ）・SECURITY.md・CHANGELOG.md・docs/remote-mcp-ja.md（再現手順205行）・examples/cloudflared/config.yml
  - KICKOFF/START-HEREをPhase 4（公開）へ更新。**スマホ投函口は共有サービスにしない／外向きは書き込み専用**を決定として明文化
  - テスト241件緑／lint・strict型チェック・build緑

- [x] 🔎 **公開後の最終チェックをエージェント4本で実施**（2026-09-02）
  - npm実インストール検証: 7項目全OK／稼働・防御チェック: 全OK（公開URLは認証なし401・MCP無応答）
  - 記事とREADMEの裏取りで**「npm未公開」の記述の取り残しを発見**→README3か所と記事を公開済みに修正
  - セキュリティレビュー: 重大0・中1・軽微2 → その場で修正（テスト**242件**緑）
    - 受信箱に**上限100件**を追加（認証を持つ相手にもディスクを食い潰させない・`MAX_DEPOSITS`）
    - fs系エラーの文言（ホームのパス入り）を外に返さない（deposit-mcpのerrorTextをallowlist化）
    - /healthzの無認証応答からツール名を削除（`{ok:true}`のみ）
  - 残存リスクとして記録: 預けた本文経由のプロンプトインジェクション（構造面は防御済み・読む側LLMの追従は原理的に残る）
  - 投函口サーバーは修正版で再起動済み

## 🔨 いまやっていること（公開の残り）
- [x] tarball実機テスト → GitHubへコミット・push
- [x] Zenn記事published（2026-09-01）: https://zenn.dev/shoujiki_panman/articles/session-relay-no-handoff ／READMEも読者の場面から始まる構成に改稿
- [x] npm publish 0.2.0（2026-09-01）: `@shoujiki-panman/session-relay`。🐛アカウント名は `shojikipanman`（ハイフン無し）でscope不一致→npm組織 `shoujiki-panman` をFreeで新設して解決
- 記事への実機スクショ掲載は本人判断でなし（会話の中身が見えるため）。デモ動画も作ったが本人判定で不採用→デモ用の偽会話ごと削除済み
- [x] X告知（2026-09-02・正直パンマン名義）: 「枠はツールごとに別」を編集で補記。BootCamp Slackにも共有
- 📊 使われ方の数字は npm DL（1日遅れ）／GitHub star・clone（cloneはbot混じり）／Zennいいね／Xアナリティクスで見る。公開直後実測: clone 25/uniq 19・star 0
- MulmoTerminalで「会話の続き」を開いて、タップ→下書き→送信 が通るかを**本人が**確かめる
- Codexの**対話TUI**で「続きから」→ MCP承認 → 現在地が答えられるかを確かめる（headlessは落ちる・上の⚠️）
- 投函口サーバー＋Tunnelの**常駐化**（いまは手動起動。Macを再起動すると止まる）と、利用者ごとのレート制限

## ⚠️ いまの限界（分かっていて残していること）
- gitの欄は**relayを打ったディレクトリ**のリポジトリを映す。会話の話題と別のリポジトリのことがある
  （実測: cwdがai-samples、作業はsession-relay）。渡した先も同じcwdで開くので嘘ではないが、`場所:` を明記して区別できるようにした
- 触ったファイルの一覧は Read/Edit/Write の入力からしか採れない。**Bashだけで作業すると空に近くなる**（この会話がまさにそう）
- ✅ **調査済み（2026-08-27）: CodexはセッションIDを環境変数で渡してこない。** `codex` バイナリが持つ `CODEX_` 系は `CODEX_APPLY_GIT_CFG` / `CODEX_CI` / `CODEX_INTERNAL_ORIGINATOR_OVERRIDE` の3つだけ（codex-cli 0.147.0 を実測）
  - 代わりに記録の**先頭行が `type: session_meta`** で、`payload.session_id` と `payload.cwd` を持つ。置き場所は `~/.codex/sessions/YYYY/MM/DD/rollout-<ISO時刻>-<uuid>.jsonl`（ファイル名に時刻とidが入る／実測51件）
  - つまり **Claude Code ほど確実には特定できない**。Codex側は「cwd一致＋新しい順」の当て推量に頼るしかない
  - ただし Codex 自身も `codex resume --last` で「直前」を時刻で決めている。**同じ割り切りで良い**という根拠になる

- [x] スキルのハーネス跨ぎ対応（2026-08-26）: 自作7本を`~/.codex/skills`へシンボリックリンク。Codex側で認識を実測確認。**作るものはゼロだった**
- [x] ロードマップの置き場所を決定（正本はKICKOFF/STATUSの1箇所・READMEは公開時）／中止したアプリ専用スキル削除

## ⏭ 残り
- Phase 4で README にロードマップを載せる（それまで置き場所を増やさない）
- Phase 2の残り: インデックス（SQLite + FTS5）→ Phase 3 MCP＋自動配達 → Phase 4 公開
- Phase 3の設計変更: 「引ける」だけでなく**新セッション起動時に自動で宙に浮いた件を知る**導線（Claude CodeはSessionStartフック、Codex側は要調査）

## ⚠️ ハマりどころメモ
- Claude Codeの`message.content`は**文字列とブロック配列の両方**を取る。片方だけ見ると発話を大量に取りこぼす（実測: 11件しか取れず、正しくは80件）
- `custom-title`が付いているのは**11%**だけ。索引に当てにできない
- Codexは`event_msg`の`payload.type=="user_message"`に`message`が入る。`~/.codex/config.toml`は既に`[mcp_servers.*]`を持つ＝MCP対応済み

## 💡 これからの候補（2026-08-26 追記・未着手）

### A. 普通のClaude/GPTチャットとの相互移動
方向で難易度が真逆。調査済み。
- ~~**① ターミナル → Web チャット（簡単・30分）**~~ → **コードを書かずに解決**（2026-08-26）。ヘッダーボタンで `relay --print | pbcopy`。`--to clipboard` フラグは**作らなかった**
- **② Web チャット → ターミナル（難しい）**: 「1会話だけ、いますぐ」取り出す公式APIが Claude/ChatGPT どちらにも無い
  - 公式データエクスポート = 全会話まとめてZIP・メールで数時間後
  - ブラウザ拡張 = 1会話ずつ即出せるが他人の拡張に依存
  - **自分用の現実解 = Chrome MCPで開いているタブから直接読む**
  - ⚠️ OSSで配るときは「拡張やMCPが要る」＝ハードルになる。正直に書く
- 構造の違い: Webチャットには cwd もファイルもコマンドも無い。運ぶのは会話だけで、**人間の発話の割合はずっと高い**（射影はより単純になる）

### B. スマホのリレーアプリ → ⛔**アプリは作らない。ただし「何も作らなくていい」ではない**（2026-08-26 実測）
実機で検証した結果、**サーバー基盤（Supabase＋APNs＋iOSアプリ）は丸ごと不要**と判明。ただし**手順は実際にひどく面倒だった**（本人の言葉:「めちゃくちゃめんどかった」）。約10手かかっている:
Macがスリープ→電源→Dispatch設定→タブ違い→コマンド打ち間違い→URLで接続→新セッションは会話を知らない→AirDrop→Notes→スマホで全選択コピー→貼る。
**これは成功ではなく、直すべき摩擦**。下の「次に作るもの」がその答え。
- ✅ **操作はRemote Controlで解決**: `claude --remote-control` / `/remote-control` → スマホのClaudeアプリから母艦のセッションに入って会話できる（実測）。移すのでなく同じ会話に別の入口から入るので千切れない
- ⚠️ **ただし新しいセッションは会話を知らない**ので、relayは別途必要（Remote Controlが解いたのは「どこから操作するか」だけ）
- ✅ **受け渡しはApple Notes（iCloud同期）で足りた**: `relay --print` → Notesに入れる → スマホでコピペ。**サーバーもプッシュ基盤も不要**（前のアプリで作ったものが丸ごと要らなかった）
- 🎯 **次に作るもの（摩擦をゼロにする本命）: `relay` スキル**
  スマホから一言「続きから」と打つだけにする。セッションはMacの上で動いているので、**自分で `relay` を実行して前の会話を読める**。ファイルもNotesもコピペも不要（＝今日の10手が1手になる）
  - 必要な追加: **`relay --print --previous`**（新しいセッションが「自分ではなく直前の会話」を引くためのオプション）。現状は `CLAUDE_CODE_SESSION_ID` で自分を掴むので、新セッションからだと空の自分を返してしまう
  - `--to notes` / `--to clipboard` はその後でいい（スキルが効けば出番が減る）
- ⚠️ Remote Controlの弱点（実測）: **Macが起きてオンラインである必要がある**。蓋を閉じると寝る（`sleep 1`設定・caffeinateの`-i`もClaudeのトグルも蓋閉じは防げない）。外出中に使うなら電源＋Capsomnia が前提

### （旧）スマホのリレーアプリ（構想・保留）
スマホから会話を移す／スマホで続ける。
- 🚨 **前のアプリと同じ罠**: セッションの実体はMacにあるので、アプリにするには同期基盤（サーバー＋プッシュ）が要る。前回そこに時間を使って中止した
- **より安い道が既にある**: Claude Code公式の **Remote Control**（Claudeモバイルアプリからセッションを操作できる。ターミナルにも `/remote-control` のヒントが出る）＋ `relay` コマンド
  → スマホから「relayして」と言えば移せる＝**アプリを作らずに8割**
- 判断: まずデスクトップで毎日使う。**スマホ側で本当に足りないことが特定できてから**作る（先にアプリを決めると、また器に中身を合わせることになる）

### 一覧を1枚のHTMLにする `relay --page`（2026-08-30）
本人の指摘:「この機能はmulmoclaudeでやりたいわけではなく、**どのツールでもすぐに連携できるようにしたい**」。
直前まで MulmoClaude のコレクションと MulmoTerminal のヘッダーボタンに作っていたが、**それはそのアプリを使っている人しか選べない**。同じ間違いを繰り返さないための記録。

- `relay --page [出力先]` → 一覧を1枚のHTMLにして既定のブラウザで開く。既定の置き場は `~/.cache/session-relay/conversations.html`（打った場所のリポジトリを汚さないため）
- 実測: プロジェクト12 / 会話165件 / 32KB / **外部URL 0件**（CDNもフォントも取りに行かない＝ネットが無くても、どの端末でも同じように開く）
- 押すとクリップボードに乗るのは**指示の一行**。会話の本文はページに焼き込まない ── 焼き込むと、そのファイルを人に見せた瞬間に鍵や個人情報ごと渡すことになる
- MCPを積んでいないツール向けに、行ごとに「端末から」（`relay --from <ref>` をコピー）も置いた
- 罠を2つテストで固定: 見出しに `</script>` が入るとページがそこで閉じる／`String.replace` に文字列で差し込むと見出しの `$&` が化ける
- ⚠️ **画面は本人の確認待ち**。書き出し・件数・外部URL0件までは確認したが、ブラウザでの描画とコピーの成否は見ていない
- ❌ **本人の判定:「ブラウザ形式だるいな」**（2026-08-30）。開く・切り替える・貼る の3手が残るので、結局この道は主役にならない。**主役は「言うだけ」**（`get_context` の `about` に本人の言葉をそのまま渡す）で、ページ・ボタン・コレクションは「思い出せないとき」の保険

### 一覧の8割がゴミだった（2026-08-30・上と同じ日に発覚）
「だるい」と言われて `list_conversations` を実際に画面へ出したら、mulmoclaude の87本のうち**50本以上**が
`SECURITY BOUNDARY: the blocks below are passed-through data…` で始まる自動実行だった。
- 8/29に入れた `INJECTED` は `^this is an automated run` の**先頭一致**。この型は「automated run」が前置きの**後ろ**に来るので素通りしていた
- 教訓: **除外規則は、実データを画面に並べて初めて足りないと分かる**。テストが緑でも一覧が汚ければ、どんな見せ方をしてもだるい
- `INJECTED` に `^security boundary\b` を追加し、cache の `SHAPE` を 3→4（更新時刻が同じでも覚え書きを捨てさせる）。テスト218件緑
- ⚠️ `~/.cache/session-relay/conversations.html` はこの修正前に作ったものなので古い。`relay --page` を打ち直すと消える

### 見出しに出ない言葉でも呼べるようにした（2026-08-30）
「言うだけ」が主役だと決めたので、**言って当たる範囲**を広げた。それまで `findByWords` は
見出しと置き場所しか見ておらず、見出しが `(発話なし)`（スクショ1枚）や `github.com`（URLを貼っただけ）の
会話は**どんな言葉でも呼べなかった**。見出しは最初の1発話・44文字なので、話の中身は大半が範囲外にある。

- `Listed` に `words` を追加（発話を整形して連結・小文字・**400文字まで**）。`describe()` が作り、覚え書きにそのまま乗る
- 全文は覚えない。`~/.cache` が会話の写しになるのを避けるため。当てるのに要るのは書き出しだけ、という見立て
- ハーネスの流し込みは `words` に混ぜない（混ぜると全部が同じ言葉で当たる）
- 覚え書きの `SHAPE` を 4→5。`hasShape` でも `words` を要求する
- 実測: 「ブラウザ」で3件当たるようになった。**当たった3件の見出しはどれも「ブラウザ」を含まない**（`DOM構造って何？` など）＝以前は0件だった範囲
- テスト221件緑（`words` の3件を追加: 2つ目以降の発話・44文字より後ろ・流し込みを混ぜない）

### `relay --pick` を MulmoTerminal のボタンに（2026-08-30）
`--pick`（↑↓・打つと絞る・Enter）は前からあったが、**呼ぶ入口が無いので使われていなかった**。
`~/.mulmoterminal/config.json` と `mulmoclaude/.mulmoterminal.json` に `relay-pick` ボタン（`relay --pick --all`）を追加。
全体設定はファイル直編集なので**反映にはMulmoTerminalの再起動が要る**。
