# 証跡: ハーネス跨ぎの引き継ぎ（2026-08-26）

渡したもの: 射影 17.8KB のみ（引き継ぎ書なし・メモリなし・リポジトリ非公開）
実行: `cd /tmp && codex exec --sandbox read-only --skip-git-repo-check < 射影`

## Codex（別ハーネス）の回答

1. 今は「session-relay」プロジェクトです。Claude CodeとCodexの間で、引き継ぎ書を書かずに会話・作業文脈を移す仕組みを検討中です。直近では計画を作り、`codex exec` と `claude -p` を使った受け渡しテストまで進めています。

2. 前のアプリは一旦停止しました。iOSアプリ、設計図表示、Live Activity、同期、Push通知、CIなどは作りましたが、本当の課題は「進捗を可視化すること」ではなく「AIツールを切り替えても会話をそのまま継続できること」だと分かったためです。本人の判断は「前のアプリに価値はない」でした。

3. 次は、session-relayで実際のClaude CodeセッションをCodexへ渡し、そのまま作業を継続できるかEnd-to-Endで試すことです。まずは自分用のCLIとして成立させ、抜け漏れ・秘密情報・ツール固有情報の扱いを確認するのが次の一手です。
