# 証跡: 結果の信号を足したら、進捗を過小評価しなくなった（2026-08-26）

## 何を確かめたか
Phase 1の射影は**本人の発話しか運ばない**ため、渡された先が進捗を過小評価していた。
（前回の実験での誤り: テストが緑なのに「まだ動く証明はできていない」と読んだ）

Phase 2で「各ターンの最後にAIが報告した文（直近10ターン）」と「渡した時点のgit」を足し、
**文脈ゼロの新セッション**に射影だけを渡して同じことを聞いた。

## 条件
- `relay --print` の出力（33,650バイト）だけを渡した。引き継ぎ書・メモリ・リポジトリへのアクセスなし
- 実行場所は `/tmp`、`claude -p --model claude-opus-5`
- 「ファイルを一切開かず、コマンドも実行せず、記録だけで答えること」と指示
- ⚠️ この射影は同じディレクトリで動いていた**別のセッション**のもの（当時 relay は更新時刻で選んでいたため取り違えた）。
  この取り違え自体はこのあと `CLAUDE_CODE_SESSION_ID` を使うよう直した

## 聞いたこと
1. いま作っているものは何で、どのディレクトリにあるか
2. どこまで出来ているか。テストは通っているか（通っているなら件数も）
3. 次にやることは何か

## 答え（原文）
> 記録だけで答えます（ファイルは開いていません）。
> 
> ## 1. 何を、どこで
> 
> **session-relay** — 会話そのものを「射影」して、引き継ぎ書なしで別ハーネス・別スレッドに移せるようにする仕組み。
> 
> `.../handoff-next-chat-2026-06-24/work/session-relay/`
> 
> ※ 以前作りかけていた別アプリは**中止**。その専用スキルも削除済み。上に出ている `ai-samples` は単にcwdで、作っているものとは別です。
> 
> ## 2. どこまで
> 
> - **中心仮説は実証済み**：17.8KBの射影だけで、文脈ゼロの新セッションが会話全体・中止理由・未回答の質問まで復元。**Claude Code と Codex の2ハーネスで再現**（証跡2本コミット済み）
> - **テスト**：抽出器のテスト31件・厳格lint・`npm run check` 緑。その後、移った先のセッションが実使用バグ2件（relay文脈の入れ子、通知の発話混入）を直して **49件（+18）** に。コミット `ce7f6f1`、作業ツリーはクリーン
> - **スキル同期**：両ハーネスで使える状態（リンク7本）。ただし `~/.codex/skills` はMulmoTerminal管理下なので消える可能性あり（未検証）
> - **ロードマップ**：正本は `KICKOFF.md` ＋ `STATUS.md` の1箇所に確定
> 
> ## 3. 次にやること
> 
> **進行中**：別セル（実装担当）が Phase 2「**結果の信号**」。いまの射影は本人の発話しか運ばないため受け手が進捗を過小評価する問題を、各ターン末尾のAI発言・git log・テスト結果を足して直す。まず量を実測してから足す方針。→ **このセルは `work/session-relay/` に書き込まない**（衝突回避の役割分担）
> 
> **その次の順番**（Phase 2のインデックスより優先と結論済み）：
> 1. CLIの `relay` コマンド（cwdから現在セッションを自動特定）
> 2. MulmoTerminalのヘッダーボタン＝ワンポチ ← ここで日常的に使える形になる
> 3. SessionStartフック（押し忘れても届く）
> 4. MCP（あとから深掘り用）
> 
> **未着手・未回答のもの**：
> - `relay --to clipboard`（ターミナル→WebのClaude/GPT、約30分）／逆方向はChrome MCP経由が現実的、という2点を `STATUS.md` に追記するか — **回答待ちのまま**
> - **リレーのスマホアプリ化** — 直前の発話で出たきり、まだ何も検討していません

## 事実と照らした結果

| 聞いたこと | 答え | 実際 | 判定 |
|---|---|---|---|
| 何を作っているか | session-relay | session-relay | ✅ |
| どこにあるか | `work/session-relay/` | 同じ | ✅ |
| cwdとの区別 | 「`ai-samples` は単にcwd」と明記 | そのとおり | ✅ |
| テスト | 49件・`ce7f6f1`・ツリーはクリーン | そのとおり（当時） | ✅ |
| 中止したもの | 前のアプリ・理由つき | そのとおり | ✅ |
| 次の一手 | Phase 2「結果の信号」 | そのとおり | ✅ |
| 未回答の質問 | 2件あると指摘 | そのとおり | ✅ |

**前回落ちていた「進捗の過小評価」は起きなかった。**
テスト件数・コミットハッシュ・作業ツリーの状態まで、当てずっぽうでなく正しく言えている。
