{
  "name": "session-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "引き継ぎ書を書かずに、AIコーディングのセッションを別ハーネスへ持ち運ぶ",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint src test",
    "typecheck": "tsc --noEmit",
    "check": "npm run lint && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.3.0",
    "eslint": "^10.9.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.68.0",
    "vitest": "^4.1.11"
  }
}
